diff --git a/evals/README.md b/evals/README.md
new file mode 100644
--- /dev/null
+++ b/evals/README.md
@@ -0,0 +1,25 @@
+# Eval sidecars
+
+A sidecar stores residue from an eval run. It is not a second source of truth for the Bend program.
+
+Naming
+`evals/name.bend` may have `evals/name.sidecar.json`.
+
+Write only facts produced by a run or by an explicit comparison:
+- stable diagnostic ids
+- graph roots actually inspected
+- proof moves actually tried
+- residual goals or distinctions still open
+- irreversible boundaries discovered
+- evidence locations
+- the cheapest next discriminator
+- a concrete reopen trigger
+
+Never convert an unresolved distinction into a decision to make the file look complete.
+
+Status meanings
+SOLVED: target closed by the current evidence.
+ROBUST: target remains closed under the recorded relevant perturbations.
+BRANCHED: materially different live outcomes remain.
+UNRESOLVED: a distinction can change the result and has not been discriminated.
+BLOCKED: the next discriminator cannot currently be executed.
diff --git a/evals/_sidecar.schema.json b/evals/_sidecar.schema.json
new file mode 100644
--- /dev/null
+++ b/evals/_sidecar.schema.json
@@ -0,0 +1,21 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "$id": "https://bend-lang.com/schema/eval-sidecar-v1.json",
+  "title": "Bend eval residue sidecar v1",
+  "type": "object",
+  "required": ["schema","target","status","residuals","evidence"],
+  "properties": {
+    "schema": { "const": "bend.eval-sidecar.v1" },
+    "target": { "type": "string" },
+    "status": { "enum": ["SOLVED","ROBUST","BRANCHED","UNRESOLVED","BLOCKED"] },
+    "diagnostics": { "type": "array", "items": { "type": "string" } },
+    "graphRoots": { "type": "array", "items": { "type": "string" } },
+    "movesTried": { "type": "array", "items": { "type": "string" } },
+    "residuals": { "type": "array", "items": { "type": "string" } },
+    "irreversibleBoundaries": { "type": "array", "items": { "type": "string" } },
+    "evidence": { "type": "array", "items": { "type": "string" } },
+    "nextDiscriminator": { "type": ["string","null"] },
+    "reopenTrigger": { "type": ["string","null"] }
+  },
+  "additionalProperties": false
+}
diff --git a/evals/_template.sidecar.json b/evals/_template.sidecar.json
new file mode 100644
--- /dev/null
+++ b/evals/_template.sidecar.json
@@ -0,0 +1,13 @@
+{
+  "schema": "bend.eval-sidecar.v1",
+  "target": "evals/example.bend",
+  "status": "UNRESOLVED",
+  "diagnostics": [],
+  "graphRoots": [],
+  "movesTried": [],
+  "residuals": [],
+  "irreversibleBoundaries": [],
+  "evidence": [],
+  "nextDiscriminator": null,
+  "reopenTrigger": null
+}
