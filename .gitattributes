# Unified diffs prefix empty context lines with a space. The installer checks
# whitespace in the applied content with git apply --whitespace=error.
patches/*.patch whitespace=-blank-at-eol,-blank-at-eof
